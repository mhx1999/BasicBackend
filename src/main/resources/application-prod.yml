# 线上配置文件
spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.206.0.6:3306/mhx
    username: root
    password: 12345678sQ
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0
